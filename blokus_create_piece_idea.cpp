// Kind of failed idea ofr create piece...


void neighbors(vector<int>* checked) {

  // checked --> {current index being checked (base t.dimension), row/col adjust (base2), which adjustment (base 2)}
  // bases -->  {bases (t.dimension), base 2, base 2}
  vector<int> adj = {-1, 1};

  for ()

}


for (int k = 0; k < t.shape; k++) {
  vector<int> bases

  int neighbors = 0;
  while (neighbors < ())

}